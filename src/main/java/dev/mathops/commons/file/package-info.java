/**
 * Utility methods to load data from files or URLs (which may or may not be zipped) and return the data as byte arrays
 * or strings, and to write byte arrays or strings to files (uncompressed or zipped).
 */
package dev.mathops.commons.file;
