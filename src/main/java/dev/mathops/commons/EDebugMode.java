package dev.mathops.commons;

/**
 * A debug mode.
 */
public enum EDebugMode {

    /** Normal mode - actions performed as designed. */
    NORMAL,

    /** Debug mode - actions not taken but log messages generated to indicate what actions would have occurred. */
    DEBUG;
}
